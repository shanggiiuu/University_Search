package com.tumo.finalproject.model;

/**
 * One message in a chat conversation — either something the user said or
 * something the assistant replied.
 *
 * <p>Chat APIs model a conversation as a list of these: every message has a
 * {@code role} saying who is speaking, and {@code content} holding the text.
 *
 * <h2>TODO 1 — declare the fields (all private)</h2>
 * <pre>
 *   String role      who is speaking: "system", "user" or "assistant"
 *   String content   the message text
 * </pre>
 *
 * <h2>TODO 2 — add two constructors</h2>
 * <ul>
 *   <li>A no-argument constructor.</li>
 *   <li>A constructor taking {@code (String role, String content)}.</li>
 * </ul>
 *
 * <h2>TODO 3 — add a getter and a setter for both fields</h2>
 */
public class ChatMessage {

    // TODO: fields, constructors, getters and setters go here.
}
